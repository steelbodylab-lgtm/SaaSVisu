# Saas Visu — dépendances Python
# Core
pydub>=0.25.1
python-dotenv>=1.0.0
# API web
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6
requests>=2.28.0
# Transcription + calage pro (moteur principal)
assemblyai>=0.50.0
# Séparation vocale (isole les voix de la musique avant transcription)
demucs>=4.0.0
# Whisper local (fallback)
faster-whisper>=1.0.0
openai-whisper>=20231117
# Beat detection
librosa>=0.10.0
