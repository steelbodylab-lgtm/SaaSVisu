# SaaSVisu

**Branche par défaut (affichage GitHub).**  
Tout le code et le travail se font sur la branche **`master`**.

Après un clone :

```bash
git checkout master
```

Puis suivre **WORKFLOW_COLLABORATION.md** (pull/push sur `master`).
